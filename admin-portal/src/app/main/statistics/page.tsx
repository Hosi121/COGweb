import { DataVisualizer } from '@/components/statistics/DataVisualiser';

export default function StatisticsPage() {
  return (
    <main className="container mx-auto px-6 py-8">
      <DataVisualizer />
    </main>
  );
}